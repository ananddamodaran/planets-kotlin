<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="title"
            type="String" />

        <variable
            name="description"
            type="String" />
        <variable
            name="thumbnailImage"
            type="String" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"

        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?android:attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true"
        android:minHeight="@dimen/planet_list_item_min_ht"
        >

        <TextView
            android:id="@+id/title_tv"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:textColor="@color/title_color"
            android:textSize="@dimen/title_txt_size"
            app:layout_constraintBottom_toTopOf="@+id/summary_tv"
            app:layout_constraintEnd_toStartOf="@+id/thumbnail"
            android:layout_marginTop="@dimen/margin_top"
            android:layout_marginEnd="@dimen/margin_end"
            android:text="@{title}"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="MERCURY" />

        <TextView
            android:id="@+id/summary_tv"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/summary_margin_start"
            android:layout_marginTop="@dimen/summary_margin_top"
            android:layout_marginEnd="@dimen/summary_margin_end"
            android:ellipsize="end"
            android:maxLines="2"
            android:textSize="@dimen/summary_text_size"
            android:text="@{description}"
            app:layout_constraintHorizontal_bias="0.5"

            app:layout_constraintEnd_toEndOf="@+id/title_tv"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/title_tv"
            tools:text="Mercury is the smallest and innermost planet in the solar .." />

        <ImageView
            android:id="@+id/thumbnail"
            android:layout_width="@dimen/thumbnail_w_h"
            android:layout_height="@dimen/thumbnail_w_h"
            android:layout_marginEnd="@dimen/thumbnail_margin_end"
            android:background="@color/thumbnail_bg"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:imageFromUrl="@{thumbnailImage}" />
    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>


